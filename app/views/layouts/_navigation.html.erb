<%= link_to "TMS", root_path, :class => 'navbar-brand' %>
<ul class="nav navbar-nav">
  <% if user_signed_in? %>
    <% if can? :read, User, :index => true %>
      <%= nav_link 'Manage Users', users_path %>
    <% end %> 
    <% if can? :create, User %>
      <%= nav_link 'Create User', add_user_path %>
    <% end %>
    <% if can? :import_users, User %>
      <%= nav_link 'Import Users', import_users_path %>
    <% end %>
    <% if can? :create, Project %>
      <%= nav_link 'New Project', new_user_project_path(current_user) %>
    <% end %>
  <% end %>
</ul>

<ul class="nav navbar-nav pull-right">
  <% if user_signed_in? %>
    <%= nav_link 'My Account', edit_user_registration_path %>
    <li>
    <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <%= nav_link 'Login', new_user_session_path %>
  <% end %>
</ul>
